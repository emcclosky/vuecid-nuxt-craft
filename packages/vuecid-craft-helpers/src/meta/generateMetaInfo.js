/* eslint-disable no-console */

/**
 * Take data from seomatic and return a more useful object to further process.
 *
 */

export default function metaDataTransform({ seomaticMeta = {} } = {}) {

  return {
    title: 'generated right here'
  }
}
