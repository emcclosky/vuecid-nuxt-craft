#import "./fragments/flexibleContent.gql"

query ($slug: String!) {
  entries(section: [page], slug: $slug) {
    ... on PageFlexContent {
      id
      title
      slug
      flexibleContent {
        ...text
        ...image
        ...teaser
      }
    }
  }
}
