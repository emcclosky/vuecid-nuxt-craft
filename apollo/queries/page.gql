#import "./fragments/seomatic.gql"

query Page($slug: String!) {
  entries(section: [pages], slug: $slug) {
    slug
    title
    ...on Pages {
      richtext {
        totalPages
        content
      }
    }
  }
}
